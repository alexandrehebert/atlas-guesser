services:
  guesser:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: guesser-app
    ports:
      - "4102:4102"
    environment:
      NODE_ENV: production
      PORT: 4102
    restart: unless-stopped
